{
  "author": "shu@hexlink.io",
  "description": "Utilities and Classes for hexlink contracts",
  "main": "./lib/index.js",
  "module": "./lib.esm/index.js",
  "types": "./lib/index.d.ts",
  "name": "@hexlink/common",
  "scripts": {
    "build": "rimraf lib && rimraf lib.esm &&  tsc --build ./tsconfig.json && tsc --build ./tsconfig.esm.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "sideEffects": false,
  "version": "0.1.5",
  "dependencies": {
    "bignumber.js": "^9.1.1",
    "ethers": "^5.7.2"
  },
  "files": [
    "/lib"
  ]
}
