{
  "author": "shu@hexlink.io",
  "description": "Utilities and classes for hexlink redpacket contract",
  "main": "./lib/index.js",
  "module": "./lib.esm/index.js",
  "name": "@hexlink/redpacket",
  "scripts": {
    "build:node": "rimraf lib && rimraf tsconfig.tsbuildinfo && tsc --build ./tsconfig.json",
    "build:es": "rimraf lib.esm && rimraf tsconfig.esm.tsbuildinfo && tsc --build ./tsconfig.esm.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "sideEffects": false,
  "types": "./lib/index.d.ts",
  "version": "0.1.2",
  "dependencies": {
    "@types/node": "^18.11.18"
  },
  "devDependencies": {
    "typescript": "^4.9.4"
  },
  "private": true
}
