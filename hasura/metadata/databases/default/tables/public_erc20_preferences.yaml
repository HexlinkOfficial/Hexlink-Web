table:
  name: erc20_preferences
  schema: public
insert_permissions:
  - role: user
    permission:
      check:
        user_id:
          _eq: X-Hasura-User-Id
      columns:
        - display
        - chain_id
        - id
        - address
        - display_name
        - user_id
        - created_at
        - updated_at
select_permissions:
  - role: user
    permission:
      columns:
        - address
        - chain_id
        - created_at
        - display
        - display_name
        - id
        - updated_at
        - user_id
      filter:
        user_id:
          _eq: X-Hasura-User-Id
update_permissions:
  - role: user
    permission:
      columns:
        - display
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      check: null
