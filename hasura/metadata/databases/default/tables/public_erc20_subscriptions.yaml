table:
  name: erc20_subscriptions
  schema: public
insert_permissions:
  - role: user
    permission:
      check:
        user_id:
          _eq: X-Hasura-User-Id
      columns:
        - address
        - chain_id
        - created_at
        - id
        - updated_at
        - user_id
select_permissions:
  - role: user
    permission:
      columns:
        - address
        - chain_id
        - created_at
        - id
        - updated_at
        - user_id
      filter:
        user_id:
          _eq: X-Hasura-User-Id
update_permissions:
  - role: user
    permission:
      columns:
        - address
        - chain_id
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      check: null
delete_permissions:
  - role: user
    permission:
      backend_only: false
      filter:
        user_id:
          _eq: X-Hasura-User-Id
